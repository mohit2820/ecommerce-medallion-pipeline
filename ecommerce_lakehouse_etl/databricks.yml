# This is a Declarative Automation Bundle definition for ecommerce_lakehouse_etl.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: ecommerce_lakehouse_etl
  uuid: 02afa6ba-e380-4bab-bf96-12ae8f88882b

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-404769e6-c79b.cloud.databricks.com
    variables:
      catalog: ecommerce_catalog
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-404769e6-c79b.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/bittuchudhare9118@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/bittuchudhare9118@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: ecommerce_catalog
      schema: prod
    permissions:
      - user_name: bittuchudhare9118@gmail.com
        level: CAN_MANAGE
